const fruits = ['mango', 'papaya', 'pineapple', 'apple'];

// Iterate over fruits below
fruits.forEach(fruit => console.log(`I want to eat a ${fruit}.`))

/* 
itera sobre la array fruits con el metodo .forEach y dentro de
este metodo definimos la funcion fruit sin parametros que nos devuelve
el string + cada fruta de la lista en cada frase.
*/