// Funcion que recibe dos numeros como parametros y nos devuelve su suma

function sumProof (n1, n2) {
    return n1 + n2;
}

// Funcion que recibe dos cadenas como parametros y nos la devuelve contatenadas

function stringProof(str1, str2) {
    return str1 + str2;
}

// funcion que recibe un numero y te devuelve un booleano

function numProof (n1, n2) {
    if (n1 > n2);
}

sumProof(1,2);
stringProof('hola', 'mundo')
console.log(sumProof)
console.log(stringProof)